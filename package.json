{
  "name": "belfast-replay-ui-api",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight local UI/API scaffold for the Belfast historical replay data.",
  "scripts": {
    "build:events": "node scripts/build_infrastructure_events.js",
    "build:manifest": "python scripts/build_ui_manifest.py && python scripts/build_mode_a_replay.py",
    "build:forecast": "python scripts/train_forecast_model.py",
    "build:transformer-model": "python scripts/build_transformer_model.py",
    "start": "node server.js",
    "verify": "node scripts/verify-manifest.js && node scripts/verify-forecast.js && node scripts/verify-transformer-model.js",
    "verify:browser": "node scripts/browser_smoke.js",
    "test": "npm run build:manifest && npm run build:forecast && npm run verify"
  },
  "devDependencies": {
    "playwright": "^1.51.1"
  }
}
